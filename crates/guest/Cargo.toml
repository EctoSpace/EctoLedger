[package]
name = "ectoledger-guest"
version = "0.5.2"
edition = "2024"

[dependencies]
ectoledger-core = { path = "../core" }
sp1-zkvm      = "6.0.1"
serde         = { version = "1", features = ["derive"] }
bincode       = "1"
regex-lite    = "0.1"