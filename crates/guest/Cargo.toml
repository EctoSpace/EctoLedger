[package]
name = "ecto-ledger-guest"
version = "0.5.3"
edition = "2024"

[dependencies]
ecto-ledger-core = { path = "../core" }
sp1-zkvm      = "6.0.1"
serde         = { version = "1", features = ["derive"] }
bincode       = "1"
regex-lite    = "0.1"